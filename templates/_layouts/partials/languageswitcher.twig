{% if element %}
    <ul class="list-none">
        {% for element in element.localized.all %}
            <li class="nav-item">
                <a class="nav-link" href="{{ element.url }}" @click="isOpen = false">{{ element.language[:2] }}</a>
            </li>
        {% endfor %}
    </ul>
{% endif %}

