{% if block.highlightBlock %}
	<div class="bg-primary text-white text-lg leading-relaxed p-3">
        {{ block.text|striptags }}
    </div>
{% else %}
    <div class="rich-text">
        {{ block.text|striptags|parseRefs|md }}
    </div>
{% endif %}


