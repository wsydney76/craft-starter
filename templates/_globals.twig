{% set global_siteName = siteInfo.siteName ?? systemName %}
{% set global_siteIcon = siteInfo.siteIcon.one %}
{% set global_titleSectionClass = "bg-primary text-white" %}
{% set global_navBlocks = siteNavigation
    .primaryNavigation
    .with([
    'topLevelLinks:entries',
    'dropDown:entries'
])
    .all %}
{% block html %}{% endblock %}
